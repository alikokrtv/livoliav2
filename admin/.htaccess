Options -Indexes
RewriteEngine On

# Statik dosyalara dokunma
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tüm admin isteklerini index.php'ye yönlendir
RewriteRule ^(.*)$ index.php [QSA,L]
